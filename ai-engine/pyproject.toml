[project]
name = "ai-engine"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "pydantic-settings",
  "anthropic",
  "jinja2",
  "asyncpg",
  "structlog",
]

[dependency-groups]
dev = ["pytest", "pytest-asyncio"]
