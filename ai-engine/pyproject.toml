[project]
name = "ai-engine"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "pydantic-settings",
  "anthropic",
  "jinja2",
  "psycopg[binary]",
]

[dependency-groups]
dev = ["pytest", "pytest-asyncio"]
