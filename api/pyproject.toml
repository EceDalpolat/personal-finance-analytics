[project]
name = "api"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "pydantic-settings",
  "httpx",
  "asyncpg",
  "structlog",
]

[dependency-groups]
dev = ["pytest", "pytest-asyncio"]