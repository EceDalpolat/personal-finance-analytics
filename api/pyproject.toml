[project]
name = "api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "pydantic-settings",
  "httpx",
  "psycopg[binary]",
]

[dependency-groups]
dev = ["pytest", "pytest-asyncio"]
